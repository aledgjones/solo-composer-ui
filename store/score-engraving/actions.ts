import { store } from "../use-store";
import { PartialEngravingConfig } from "./defs";

export const scoreEngravingActions = {
    update: (key: string, config: PartialEngravingConfig) => {
        store.update((draft) => {
            draft;
        });
    },
};
